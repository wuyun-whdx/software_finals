MYSQL_ROOT_PASSWORD=root123456
MYSQL_USER=radar
MYSQL_PASSWORD=radar123
DB_USERNAME=radar
DB_PASSWORD=radar123
REDIS_PASSWORD=radar-redis-dev

JWT_SECRET=replace-with-a-long-random-secret
CORS_ALLOWED_ORIGINS=https://app.reflectstars.dev,https://reflectstars.dev,http://127.0.0.1:5173,http://localhost:5173
PUBLIC_BASE_URL=https://app.reflectstars.dev
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# AI provider: mock (default, fake data) or real (DeepSeek + Amap)
AI_PROVIDER=mock

# Real provider API keys (only needed when AI_PROVIDER=real)
# AI_API_KEY = DeepSeek API key (maps to app.ai.deepseek.api-key)
# AMAP_API_KEY = Amap/高德 Web Service key (maps to ai.amap.key)
AI_API_KEY=
AMAP_API_KEY=

# Optional overrides (defaults shown)
# AI_BASE_URL=https://api.deepseek.com
# AI_MODEL=deepseek-chat
# AI_TIMEOUT_MS=25000
